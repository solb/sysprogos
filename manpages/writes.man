WRITES(1)                        TO-D/OS SYSCALLS Manual                        WRITES(1)

NAME
     writes — Write a NUL-terminated string to the SIO

SYNOPSIS
	 int writes(char *buff);

DESCRIPTION
     
RETURN VALUE
	 The number of characters that were sent


BSD                                   May 19, 2014                                   BSD
